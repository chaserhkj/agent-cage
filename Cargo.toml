[package]
name = "agent-cage"
description = "Run your coding agent inside an isolated micro VM"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.60", features = ["derive"] }
